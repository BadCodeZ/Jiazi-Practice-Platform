# 综合教资备考工作台（网页端）· 使用指南 · 正式版 V1.1

> **本文档用途与版本定义**
> 本文件是「综合教资备考」项目**正式发布归档包**中网页端（单文件 HTML）的**用户使用说明**，供本版使用者、分发者与后续维护者阅读。
> 在本归档包中，**正式版 V1.1（网页端）对应的内部构建 = md5 `7c10091b03f64b71083399b0a2c705cf`，单文件 `综合教资备考工作台.html`（约 3.10 MB），SW 版本 `artwb-v23`，内部 `meta.version = 1.1`**。
> 网页端与手机端是**两条相互独立的版本线**：网页端「正式版 V1.1」与安卓「V2.35.5」版本号互不对齐，仅通过 v2 同步信封实现跨端数据互通。
> 作者 / 署名：BadCodeZ。

---

## 一、一句话看懂

这是一个「免安装、不用注册、存在你自己浏览器里」的教资备考刷题工具。整个程序就是电脑或手机里的一个网页文件，双击或点开就能用，**没网也能刷题**。你所有的做题记录都只留在你自己的设备上，不上传、不联网、不泄露。

---

## 二、功能一览

- **核心刷题、错题、模考、知识卡全部可以纯离线使用**（数据在浏览器 `localStorage`，键名 `artwb_v2_data`）。
- **只有 AI 讲评、多端同步需要联网，且都是可选的。**
- 本轮「正式版 V1」相对早期版本**新增 / 增强**：
  - **备课模块**（十二要素编辑器 / 模板库 / 科三训练 / 中枢 Hub）；
  - **AI 复核内置题抽检入口**（`aiVerifyBatch`，对内置 3342 题做解析-答案一致性抽检）；
  - **PDF / Word 按需解析**（课标库 / 教材库上传支持 `.txt/.md/.docx/.pdf`，pdf.js / mammoth 运行时按需加载，离线降级手动粘贴；CAJ 提示先转 PDF）；
  - 全科模考（试卷蓝图）、17 学科科三、习题量 3342、科三·美术 263、科三·语文 151。

---

## 三、怎么打开

- **电脑**：双击 `综合教资备考工作台.html`，用 **Chrome / Edge** 打开（推荐，门禁与视觉基于 Chromium 内核校验）。
- **手机**：用浏览器打开该文件后，「分享 → 添加到主屏幕」，即可像 App 一样从桌面启动。
- **注意**：手机微信内直接预览功能受限，务必用系统浏览器打开。
- **离线可用**：`file://` 双击打开也能用（自动降级为内存会话，关闭后不持久化）；要持久化请经 http（本地静态服务或托管）打开。

---

## 四、数据存在哪

- 只存在**打开它的浏览器 `localStorage`**（键 `artwb_v2_data`），不联网。
- 换浏览器 / 清缓存 / 换电脑看不到；把程序文件发给别人，对方拿到的是**空白工作台**（不含你的数据）。
- AI Key 单独存 `ai_key`，不进备份、不同步。

---

## 五、首次上手 3 步

1. **设目标日**：首页点「设置目标日」，选考试日期，首页出现「距教资笔试还有 N 天」倒计时。
2. **开始练习**：首页点「开始练习」或进「练习」选模式；客观题点选即判，主观题写要点看答案，答错勾选错因。
3. **复习错题**：答错题自动进错题本；在「校订 → 错题本」复习，或用「练习 → 仅复习」按间隔重练。

---

## 六、核心功能（含本轮新增）

1. **练习**：科一 / 科二 / 科三 + 学科，客观题点选即判，主观题写要点看答案，答错勾选错因（半强制 ≥1 项）。
2. **错题本**：筛选、按错因分组、错题本智能组卷（薄弱度降序、带 seed 可复现）。
3. **知识卡片**：按章节考点，可收藏；到期复习标记。
4. **全科模考蓝图**：按 `CHAPTER_WEIGHT` 加权抽约 50 题（科一 / 科二 / 科三 ≈33/33/34%），含当前科三学科，不串科。
5. **AI 讲评**：设置填 provider + Key，每题可 AI 讲解；无 Key 也能用（离线样例兜底）。
6. **多设备同步**：设置里 WebDAV（可选），支持「仅上传 / 仅下载 / 双向合并」三方向 + AES-GCM 加密（`SYNCPKG1`）。
7. **备课模块**（本轮新增）：十二要素编辑器 / 模板库 / 科三训练 / 中枢 Hub；课标库、教材库上传与「从库插入」引用；课本「按课抽取」直达编辑器。
8. **AI 复核内置题抽检入口**（本轮新增）：`aiVerifyBatch` 对内置 3342 题做解析-答案一致性抽检（校订页「AI 复核」标签），修复矛盾题（如 `bkxmspyi2jkk0tg1` 答案 B→A）。
9. **PDF / Word 按需解析**（本轮新增）：课标库 / 教材库上传支持 `.docx`（mammoth）/ `.pdf`（pdf.js）按需加载解析；`.caj` 提示先转 PDF；离线降级手动粘贴。

---

## 七、装到桌面 / 主屏（PWA 安装，离线可用）

- 经 http(s) 打开时，浏览器可「安装」为 PWA（manifest + service worker），离线缓存文档与静态资源（SW `artwb-v22`，文档类 network-first、静态 cache-first）。
- `file://` 下 PWA 不注册，需 http 环境。

---

## 八、换设备 / 备份恢复

- **导出同步包**：设置 → 数据同步 → 导出（复制文本）。
- **导入合并**：另一设备粘贴 + 同口令 → 解密合并（显示新增 / 更新数）。
- WebDAV 同步可选，填同一空间 + 口令即互通；`localStorage` 为 null 时（file:// / 隐私模式）自动降级内存会话并提示。

---

## 九、FAQ

- **白屏**：请用 Chrome / Edge 打开；硬刷新一次（Ctrl+Shift+R）以绕过旧 PWA 缓存。
- **数据丢失**：用「导入同步包」恢复；或检查是否换浏览器 / 清缓存 / 走 file:// 未持久化。
- **需联网吗**：仅 AI 讲评 / 多端同步需联网；核心刷题全离线。
- **安全性**：纯本地，不上传；同步可选零知识加密。
- **手机微信打不开**：用系统浏览器打开并「添加到主屏幕」。
- **科三切换**：设置切学科后题库 / 图谱随当前科三学科隔离。
- **PDF / Word 解析失败**：离线环境自动降级为手动粘贴；CAJ 请先转 PDF。

---

## 十、给分享者

- **单文件分发**：直接分享 `综合教资备考工作台.html` 一个文件即可（约 3.04 MB）。
- **保留署名**：保留作者 **BadCodeZ** 与「正式版 V1.1」标识。
- **覆盖 html 不影响用户数据**：用同名新 html 覆盖，浏览器 `localStorage` 中的 `artwb_v2_data` 不被动，用户记录保留。
- **四份版本须一致**：开发源 / `_preview/app.html` / 正式分享版 / `综合教资备考工作台.zip` 内嵌 md5 均为 `7c10091b03f64b71083399b0a2c705cf`，发布时务必四份同步、md5 一致。
- **版本线提醒**：网页端 V1（meta 1.0）与安卓 V2.35.5 版本号不同属正常，仅数据互通。

---

## 十一、免责声明

1. 本工具为个人备考辅助，题库含 AI 批量生成题，个别解析 / 答案可能存在争议，正式考试以官方大纲与教材为准。
2. AI 功能依赖第三方 API，需自备 Key 并自担费用；结果仅供参考。
3. 数据存于本机浏览器，作者不对因清缓存 / 换设备 / 误操作导致的数据丢失负责；请定期导出备份。
4. PDF / Word 解析依赖运行时按需加载的第三方解析器（pdf.js / mammoth），联网首次解析、离线降级手动粘贴。
5. 本软件按「现状」提供。
